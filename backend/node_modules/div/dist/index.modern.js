import { createElement } from 'react';

function _extends() {
  _extends = Object.assign || function (target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];

      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }

    return target;
  };

  return _extends.apply(this, arguments);
}

function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null) return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;

  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0) continue;
    target[key] = source[key];
  }

  return target;
}

var Div = function Div(_ref) {
  var flex = _ref.flex,
      column = _ref.column,
      reverse = _ref.reverse,
      row = _ref.row,
      wrap = _ref.wrap,
      center = _ref.center,
      children = _ref.children,
      style = _ref.style,
      rest = _objectWithoutPropertiesLoose(_ref, ["flex", "column", "reverse", "row", "wrap", "center", "children", "style"]);

  var styles = _extends({}, style);

  if (flex) styles.flex = flex;
  if (wrap) styles.flexWrap = 'wrap';

  if (row || column) {
    styles.display = 'flex';
    styles.flexDirection = row ? reverse ? 'row-reverse' : 'row' : reverse ? 'column-reverse' : 'column';
  }

  if (center) {
    styles.alignItems = 'center';
    styles.justifyContent = 'center';
  }

  return createElement("div", Object.assign({}, rest, {
    style: styles
  }), children);
};

export default Div;
export { Div };
//# sourceMappingURL=index.modern.js.map
