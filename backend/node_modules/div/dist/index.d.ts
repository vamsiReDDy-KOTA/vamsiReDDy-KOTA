import * as React from 'react';
interface Props extends React.DetailedHTMLProps<React.HTMLAttributes<HTMLDivElement>, HTMLDivElement> {
    flex?: number | string;
    column?: boolean;
    reverse?: boolean;
    row?: boolean;
    wrap?: boolean;
    center?: boolean;
    children?: React.ReactNode;
}
export declare const Div: ({ flex, column, reverse, row, wrap, center, children, style, ...rest }: Props) => JSX.Element;
export default Div;
